name: Java CI with Maven
secrets: 
  X-RAPIDAPI-KEY: '14c022e6c8msh6f5cee1caad4bacp16bc3cjsn350654d077a9'
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: echo "$X-RAPIDAPI-KEY"
      

